
Set valid email and password in eis-test-task\src\main\resources\config.properties 

The project generates emailable-report.html at: eis-test-task\target\surefire-reports\
and log files at: eis-test-task\target\logs 

Two maven profiles are: default (runs testSuite) and "noTests" (builds only without running tests).
